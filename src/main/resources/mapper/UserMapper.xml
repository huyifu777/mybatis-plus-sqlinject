<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.mybatisp.mapper.UserMapper">

    <select id="select3" parameterType="java.lang.String" resultType="com.example.mybatisp.entity.User">
        SELECT * FROM user WHERE  name = #{name}
    </select>
    <select id="select4" parameterType="java.lang.String" resultType="com.example.mybatisp.entity.User">
        SELECT * FROM user WHERE  name = '${name}'
    </select>
</mapper>
